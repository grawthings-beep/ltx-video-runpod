PORT=8188
LISTEN=0.0.0.0
RUN_DEP_CHECK=0
DOWNLOAD_MODELS=1
MODEL_DOWNLOAD_MODE=background
HF_TOKEN={{ RUNPOD_SECRET_HF_TOKEN }}
CIVITAI_TOKEN={{ RUNPOD_SECRET_CIVITAI_TOKEN }}
MODEL_MANIFEST_URL=https://raw.githubusercontent.com/YOUR_GITHUB_USER/YOUR_REPO/main/config/ltx-video-models.json
ARIA2_CONNECTIONS=8
ARIA2_SPLITS=8
# Number of model files to download in parallel at startup. Each file still uses
# ARIA2_CONNECTIONS connections, so total streams = DOWNLOAD_JOBS * ARIA2_CONNECTIONS.
# More streams are not always faster; raise gradually and lower if a host returns 429.
DOWNLOAD_JOBS=3
# once: verify a SHA once and cache the result; always: re-read on every boot; never: skip.
VERIFY_MODEL_HASHES=once
COMFYUI_ARGS=--reserve-vram 5
